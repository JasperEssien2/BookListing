<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="book_info_layout_style">
        <item name="android:padding">@dimen/padding_16dp</item>
    </style>

    <style name="night_theme">
        <item name="android:textColor">@color/colorBookInfoNightText</item>
        <item name="android:background">@color/colorBookInfoNightBackground</item>
    </style>

</resources>
